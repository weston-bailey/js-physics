const {
    clamp, 
    min,
    max
} = require('./MathLib')
const {
    randomInRange,
    randomRGBHex,
    randomRGBAHex
} = require('./RandLib')

module.exports = {
    clamp, 
    min,
    max,
    randomInRange,
    randomRGBHex,
    randomRGBAHex
}